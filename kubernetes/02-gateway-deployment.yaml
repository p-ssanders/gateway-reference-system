apiVersion: apps/v1
kind: Deployment
metadata:
  name: gateway
  namespace: gateway-reference
  labels:
    system: gateway-reference
spec:
  replicas: 1
  selector:
    matchLabels:
      app: gateway
  template:
    metadata:
      labels:
        app: gateway
        system: gateway-reference
    spec:
      containers:
      - name: gateway
        image: ssanders0/ptp-gateway@sha256:4b3a6f72be7b404d1ad456ba3e3d5bdf866f36d64bac766facb122f6d875ca67
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
        resources:
          requests:
            memory: 256M
            cpu: 100m
          limits:
            memory: 1G
            cpu: 1
        livenessProbe:
          httpGet:
            path: /livez
            port: 8080
            scheme: HTTP
        readinessProbe:
          httpGet:
            path: /readyz
            port: 8080
            scheme: HTTP
        env:
          - name: SPRING_PROFILES_ACTIVE
            value: cloud
          - name:  MANAGEMENT_SERVER_PORT
            value: "8081"
        volumeMounts:
          - name: spring-config
            mountPath: "/workspace/config"
      volumes:
        - name: spring-config
          configMap:
            name: gateway-config
          
