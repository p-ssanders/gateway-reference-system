apiVersion: v1
kind: Service
metadata:
  name: gateway
  namespace: gateway-reference
  labels:
    app: gateway
spec:
  selector:
    app: gateway
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
